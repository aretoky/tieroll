h1 Admin登録確認
.admin_confirm
  = form_with model: @admin, url: {action: :create}, local: true do |f|
    .admin_conform
      = @admin.name 
      = f.hidden_field :name 
    .admin_confirm
      = @admin.email 
      = f.hidden_field :email
    .admin_confirm
      = @admin.password
      = f.hidden_field :password
    .admin_confirm_submit
      = f.submit "修正", name: "back"
      = f.submit "登録"
